generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Assignee {
  id        String  @id @default(cuid())
  name      String
  color     String? // タグ表示用（例: #3b82f6）
  sortOrder Int     @default(0)
  calls     Call[]
}

model Call {
  id            String    @id @default(cuid())
  destination   String
  memo         String?
  assigneeId   String?
  assignee     Assignee?  @relation(fields: [assigneeId], references: [id], onDelete: SetNull)
  isAppointment Boolean   @default(false)
  /// 架電結果: APPOINTMENT=アポ, NO_ANSWER=未応答, OTHER=つながったがアポにならず
  status        CallStatus @default(OTHER)
  createdAt    DateTime  @default(now())
}

enum CallStatus {
  APPOINTMENT
  NO_ANSWER
  OTHER
}

model Goal {
  id     String @id @default(cuid())
  type   String // "week" | "month"
  value  Int
  period String // e.g. "2025-W11", "2025-03"
  @@unique([type, period])
}
